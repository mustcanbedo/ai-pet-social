# DeepSeek API 配置
# 复制此文件为 .env.local 并填入 Key：
#   cp .env.local.example .env.local
#
# 配置后重启 dev server（npm run dev）
# 页面右上角将显示「DeepSeek 模式」
#
# 安全说明：
#   Key 仅存在于 .env.local，不会提交到 Git
#   实际调用通过 Next.js API Route（/api/chat）代理，Key 不暴露到浏览器
#
# 未配置时自动降级为 Mock AI 模式（仅适合展示 UI 结构，不支持真实对话）
DEEPSEEK_API_KEY=
